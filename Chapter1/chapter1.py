'''chapter1 : Module , comments , pip'''
import os # preinstalled module
import flask #needed pip to install 
print("Hello World") #pip is package manager which provide to access various packages or modules by installing it
''' we can use python as a calculator on windowspower shell on writing any operation but we can't access it on
future because it does not save the lines of code in a file so we need to use a editor rather than a powershell window '''
 
